{
  "name": "zippopotam-api-tests",
  "version": "1.0.0",
  "description": "API test suite for Zippopotam postcode lookup service",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage",
    "test:smoke": "vitest run --grep smoke",
    "test:security": "vitest run --grep security"
  },
  "keywords": ["api-testing", "vitest", "typescript", "rest-api"],
  "author": "Test Automation Engineer",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@vitest/ui": "^1.0.4",
    "typescript": "^5.3.3",
    "vitest": "^1.0.4",
    "@vitest/coverage-v8": "^1.0.4"
  },
  "dependencies": {
    "axios": "^1.6.2"
  }
}